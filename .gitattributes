# Normalize text files to LF in the repository so Windows/Linux checkouts
# don't show every line as modified.
* text=auto eol=lf

# The packaged skill is a zip archive, not text.
*.skill binary
*.png binary
